name: tcc-metatranscriptomica
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  
  # Ferramentas bioinformáticas
  - fastqc
  - multiqc
  - trimmomatic
  - fastp
  - bowtie2
  - bwa
  - kraken2
  - bracken
  - samtools
  - bcftools
  - snpeff
  - picard
  
  # Python packages (via conda)
  - pip
  - pandas>=1.5.0
  - numpy>=1.23.0
  - scipy
  - scikit-learn>=1.2.0
  - matplotlib>=3.6.0
  - seaborn>=0.12.0
  - jupyter
  - ipykernel
  - notebook
  
  # Pip install packages (mais atualizados via pip)
  - pip:
    - xgboost>=1.7.0
    - shap>=0.41.0
    - pysam>=0.19.0
    - tqdm>=4.64.0
    - pyyaml>=6.0
